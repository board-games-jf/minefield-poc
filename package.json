{
  "name": "minefield",
  "version": "1.0.0",
  "description": "Minefield — multiplayer minesweeper in real time",
  "main": "src/server.ts",
  "scripts": {
    "dev": "partykit dev",
    "deploy": "partykit deploy",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "partykit": "^0.0.107",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  },
  "dependencies": {
    "partysocket": "^1.0.2"
  }
}
